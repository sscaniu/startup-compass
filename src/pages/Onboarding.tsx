import { StartupIntakeForm } from "@/components/forms/startup-intake-form";

export default function Onboarding() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-background via-muted/30 to-background py-12">
      <StartupIntakeForm />
    </div>
  );
}